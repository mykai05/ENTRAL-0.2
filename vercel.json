{
  "installCommand": "corepack enable && pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @entral/contracts build && pnpm --filter @entral/frontend build",
  "outputDirectory": ".next"
}
