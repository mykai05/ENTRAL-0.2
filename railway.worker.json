{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "corepack enable && pnpm install --frozen-lockfile && pnpm prisma:generate && pnpm --filter @entral/contracts build && pnpm --filter @entral/backend build"
  },
  "deploy": {
    "startCommand": "corepack enable && PROCESS_ROLE=worker pnpm --filter @entral/backend worker",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
